# =============================================================================
# Kitty Session Configuration
# Auto-loaded tabs on startup
# =============================================================================

# Default shell tab (home directory)
new_tab Home
cd ~

# Development tab
new_tab Dev
cd ~/Dev

# Config tab (for quick dotfiles editing)
new_tab Config
cd ~/.config

# Downloads tab
new_tab Downloads
cd ~/Downloads
